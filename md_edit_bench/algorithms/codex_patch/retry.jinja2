The following patch sections failed to match the current document state.
The document has been partially modified by earlier sections. Please provide corrected sections
that will match the CURRENT document state shown below.

<current_document>
{{ current }}
</current_document>

<failed_sections>
{{ failed_sections }}
</failed_sections>

For each failed section, output a corrected patch section where the context and removed lines (-)
match the CURRENT document (not the original). The added lines (+) should achieve the
same intended change.

IMPORTANT: Use @@ anchor lines to identify locations. Include enough context to uniquely
identify where the change should be made in the CURRENT document.

{% include 'format_spec.jinja2' %}

Output only the corrected patch sections in Codex patch format:
